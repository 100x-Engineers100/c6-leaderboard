================================================================================
LEADERBOARD BACKEND — COMPLETE ARCHITECTURE & IMPLEMENTATION RECORD
C7 (Cohort 7) | 100xEngineers Applied Gen AI
Last updated: 2026-03-17
================================================================================

--------------------------------------------------------------------------------
STATUS: V1 COMPLETE AND LIVE
Last updated: 2026-03-17 (post-launch fixes)
--------------------------------------------------------------------------------

All 7 stages shipped. Live leaderboard with real C7 student data, UGC post
scoring, Tally avatars, and weekly GitHub Actions sync.

Post-launch fixes applied (2026-03-17):
  - Attendance sync rewritten to use summary CSV format (report_type=20)
  - Duplicate student record (kaathrohanr typo) deleted, UGC pts merged
  - Project title updated: "C6 Leaderboard" -> "Leaderboard C7"
  - Attendance overwrite bug fixed (see PITFALL #9 below)


--------------------------------------------------------------------------------
ARCHITECTURE
--------------------------------------------------------------------------------

  [C7 Launchpad CSV]  [Images CSV]  [User_rows_ugc.csv]
          |                |                |
          v                v                v
  scripts/seed-students.mjs  (one-time, run manually)
          |
          v
  [Supabase: Leaderboard DB]  <----  scripts/sync-attendance.mjs  <-- Edmingle API
          |                   <----  scripts/sync-ugc.mjs         <-- UGC Tracker DB
          |
          v
  app/api/leaderboard/route.ts  (Next.js, deployed on Vercel)
          |
          v
  hooks/useLeaderboard.ts  (client fetch on page load)
          |
          v
  PodiumTVs + RankTable  (frontend, real data)

  GitHub Actions (sync.yml):
    - Weekly cron: Sunday 18:30 UTC (midnight IST)
    - Manual trigger: mode=sync (weekly) or mode=bulk (full historical)
    - Runs: node scripts/sync-weekly.mjs


--------------------------------------------------------------------------------
KEY CONFIG
--------------------------------------------------------------------------------

Edmingle base URL : https://100xengineers-api.edmingle.com/nuSource/api/v1
Edmingle ORG_ID   : 11240
Edmingle API_KEY  : [in .env.local only — never commit]
C7 batch_id       : 194294
C7 batch_name     : 100xEngineers_Applied Gen AI Cohort 7
C7 start date     : 2026-03-16
Attendance endpoint: GET /report/csv?apikey=&ORGID=&report_type=20
                     &start=<unix>&end=<unix>&class_id=446933
                     NOTE: report_type=55 returns 500. report_type=20 is correct.
                     Returns summary CSV: Learners, Email, ..., Present, Absent, %
                     Also contains per-session date cols (e.g. "13 Mar") but we
                     use the summary Present column only.
C7 class_id       : 446933  (required — batch_id does NOT work for this endpoint)

Leaderboard Supabase : qosndqcdupypfkfgduif (project ref)
UGC Tracker Supabase : zwtxxevribrvrwtgcfqj (project ref)
C7 cohort ID (UGC)   : 4c8458b3-a700-42b2-9cc1-f52b3045e2c3


--------------------------------------------------------------------------------
SCORING (V1 — LIVE)
--------------------------------------------------------------------------------

  Metric                        Points          Source
  ─────────────────────────────────────────────────────────────────
  Attendance (Fri + Sat)        20 per session  Edmingle /report/csv
  #0to100x valid post           20 per post     UGC tracker Post table
  Weekly consistency bonus      10 per week     UGC tracker Streak table
  ─────────────────────────────────────────────────────────────────
  total_points = attendance_pts + ugc_post_pts + consistency_bonus_pts
               + assignment_pts + hackathon_pts + capstone_pts


--------------------------------------------------------------------------------
SCORING (FUTURE — MANUAL DB ENTRY)
--------------------------------------------------------------------------------

  Metric            Points          How
  ──────────────────────────────────────────────────────────
  Assignment        0-20 each       Edit student_points in Supabase table editor
  Mini Hackathon    0-100           Edit student_points in Supabase table editor
  Mid Capstone      50 fixed        Edit student_points in Supabase table editor
  Final Capstone    200 fixed       Edit student_points in Supabase table editor
  ──────────────────────────────────────────────────────────
  No API needed. Go to Supabase → Leaderboard project → Table Editor →
  student_points → find student row → edit the relevant column directly.
  total_points recalculates automatically (GENERATED ALWAYS AS column).


--------------------------------------------------------------------------------
DATABASE SCHEMA (Leaderboard Supabase)
--------------------------------------------------------------------------------

  students
  --------
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid()
  name            VARCHAR NOT NULL
  email           VARCHAR UNIQUE NOT NULL
  avatar_url      VARCHAR                    -- Tally pre-signed URL
  edmingle_id     VARCHAR                    -- populated on first attendance sync
  ugc_user_id     VARCHAR                    -- matched from UGC tracker by email
  cohort          VARCHAR DEFAULT 'C7'
  created_at      TIMESTAMP DEFAULT now()

  student_points
  --------------
  id                    UUID PRIMARY KEY DEFAULT gen_random_uuid()
  student_id            UUID REFERENCES students(id) UNIQUE NOT NULL
  attendance_pts        INT DEFAULT 0
  ugc_post_pts          INT DEFAULT 0
  consistency_bonus_pts INT DEFAULT 0
  assignment_pts        INT DEFAULT 0        -- manual entry
  hackathon_pts         INT DEFAULT 0        -- manual entry
  capstone_pts          INT DEFAULT 0        -- manual entry
  total_points          INT GENERATED ALWAYS AS (
                          attendance_pts + ugc_post_pts + consistency_bonus_pts +
                          assignment_pts + hackathon_pts + capstone_pts
                        ) STORED
  last_synced_at        TIMESTAMP
  updated_at            TIMESTAMP DEFAULT now()

  attendance_records
  ------------------
  id                  UUID PRIMARY KEY DEFAULT gen_random_uuid()
  student_id          UUID REFERENCES students(id)
  edmingle_student_id VARCHAR
  session_date        DATE NOT NULL
  session_name        VARCHAR
  is_present          BOOLEAN NOT NULL
  created_at          TIMESTAMP DEFAULT now()
  UNIQUE (student_id, session_date)          -- dedup key

  ugc_posts_cache
  ---------------
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid()
  student_id   UUID REFERENCES students(id)
  post_url     VARCHAR UNIQUE NOT NULL       -- dedup key
  num_likes    INT DEFAULT 0
  num_comments INT DEFAULT 0
  posted_at    TIMESTAMP
  platform     VARCHAR DEFAULT 'LINKEDIN'
  synced_at    TIMESTAMP DEFAULT now()


--------------------------------------------------------------------------------
ACTUAL DATA (as of 2026-03-17, cohort day 1)
--------------------------------------------------------------------------------

  Students seeded      : 129 (not ~327 — plan estimate was wrong)
  With Tally avatars   : 129 (100% — all students have photos)
  Matched to UGC DB    : 124 / 129
  Unmatched (no UGC)   : 5 (haven't signed up to UGC tracker yet)
    - shloksharma0003@gmail.com
    - abhinav27c@gmail.com
    - amanat@fitmantra.com
    - seeretgheek@gmail.com
    - adi12ti@gmail.com
  UGC posts cached     : 10 (cohort just started)
  Students with posts  : 7
  Attendance records   : 0 (no sessions yet — cohort started March 16)


--------------------------------------------------------------------------------
FILES CREATED
--------------------------------------------------------------------------------

  scripts/
    db.mjs                -- Supabase client (leaderboard DB)
    ugc-db.mjs            -- Supabase client (UGC tracker DB)
    seed-students.mjs     -- one-time: CSV → students + student_points
    sync-attendance.mjs   -- Edmingle CSV → attendance_records → attendance_pts
    sync-ugc.mjs          -- UGC DB → ugc_posts_cache → ugc/streak pts
    sync-weekly.mjs       -- wrapper: runs both syncs back-to-back
    bulk-import.mjs       -- week-by-week historical import from C7 start date
    schema.sql            -- all 4 CREATE TABLE statements

  app/api/leaderboard/
    route.ts              -- GET /api/leaderboard, returns ranked student array

  lib/
    supabase.ts           -- lazy singleton Supabase client (serverless-safe)
    types.ts              -- Student type with full points breakdown
    dummy-data.ts         -- fallback data (used if API is unreachable)

  hooks/
    useLeaderboard.ts     -- client-side fetch hook with fallback

  .github/workflows/
    sync.yml              -- weekly cron + manual trigger

  docs/
    User_rows_ugc.csv     -- exported UGC User table (used as seed fallback)


--------------------------------------------------------------------------------
ENV VARS
--------------------------------------------------------------------------------

  .env.local (local dev):
    LEADERBOARD_SUPABASE_URL
    LEADERBOARD_SUPABASE_SERVICE_KEY
    UGC_SUPABASE_URL
    UGC_SUPABASE_SERVICE_KEY
    EDMINGLE_BASE_URL
    EDMINGLE_API_KEY
    EDMINGLE_ORG_ID
    C7_BATCH_ID

  Vercel (production — only these 2 needed for the API route):
    LEADERBOARD_SUPABASE_URL
    LEADERBOARD_SUPABASE_SERVICE_KEY

  GitHub Secrets (all 8, used by sync workflow):
    All 8 vars above


--------------------------------------------------------------------------------
STAGES COMPLETED
--------------------------------------------------------------------------------

  [DONE] Stage 1 — Supabase Setup + DB Schema
    - New Leaderboard Supabase project created
    - schema.sql run: 4 tables created
    - .env.local configured

  [DONE] Stage 2 — Seed C7 Students
    - 129 unique students seeded (5 CSV duplicates auto-skipped)
    - 129 Tally avatar URLs stored
    - 124 students matched to UGC tracker by email
    - CSV fallback implemented (docs/User_rows_ugc.csv) for when UGC DB
      permissions fail

  [DONE] Stage 3 — Attendance Sync (revised 2026-03-17)
    - report_type=55 was dead (500). Confirmed report_type=20 is the correct endpoint.
    - Parser rewritten: was wide-format date-column parser, now reads summary
      CSV columns (Email + Present). attendance_pts = present_count * 20.
    - attendance_records table no longer written to — points go directly to
      student_points.attendance_pts.
    - Default start time changed from (now - 14 days) to cohort start 2026-03-16
      so weekly sync always queries cumulative totals (critical for correctness).
    - bulk-import.mjs simplified: no longer loops week-by-week for attendance
      (one call covers all history since summary is cumulative).
    - Verified: 270 rows parsed, 124 upserted on first real run.

  [DONE] Stage 4 — UGC Sync
    - sync-ugc.mjs working
    - 10 posts synced, 7 students scored
    - Streak table queried, 0 streaks yet (day 1)

  [DONE] Stage 5 — Next.js API Route
    - GET /api/leaderboard returns 129 ranked students
    - Verified locally: correct sort, correct points, Tally avatars in response

  [DONE] Stage 6 — Frontend Integration
    - useLeaderboard hook fetches from /api/leaderboard
    - PodiumTVs shows real top-3 with real avatars
    - RankTable shows real attendance/UGC/#0to100x/total columns
    - Loading states handled

  [DONE] Stage 7 — GitHub Actions
    - sync.yml deployed
    - Weekly cron: Sunday 18:30 UTC
    - Manual sync run: completed successfully
    - Manual bulk run: completed successfully
    - Node.js upgraded to 24 to avoid deprecation warning


--------------------------------------------------------------------------------
COMMON PITFALLS (ENCOUNTERED AND FIXED)
--------------------------------------------------------------------------------

  1. COHORT ID TYPO
     Plan had wrong C7 cohort ID: 4c8458b3-a700-42b2-9cb1-f52b3045e2c3
     Actual ID in UGC DB:         4c8458b3-a700-42b2-9cc1-f52b3045e2c3
     Difference: 9cb1 vs 9cc1 (one character)
     Impact: UGC sync returned 0 users
     Fix: corrected in sync-ugc.mjs and seed-students.mjs

  2. UGC SUPABASE PERMISSION DENIED
     Error: "permission denied for schema public"
     Cause: Newer Supabase projects revoke default PostgREST schema grants.
            Even with service_role key, USAGE on public schema must be
            explicitly granted.
     Fix: Run in UGC Tracker SQL Editor:
       GRANT USAGE ON SCHEMA public TO anon, authenticated, service_role;
       GRANT SELECT ON ALL TABLES IN SCHEMA public TO anon, authenticated, service_role;
       GRANT SELECT ON ALL SEQUENCES IN SCHEMA public TO anon, authenticated, service_role;

  3. DOTENV LOADING .env vs .env.local
     dotenv/config defaults to .env, not .env.local.
     Fix: scripts use config({ path: join(process.cwd(), '.env.local') })

  4. SUPABASE CLIENT THROWS AT BUILD TIME
     lib/supabase.ts initially threw if env vars were missing at module load.
     Next.js build executes module-level code, causing build failure.
     Fix: lazy singleton — only throws at request time inside getSupabase()

  5. GSAP NULL REF ERROR ON LOAD
     usePodiumAnimation ran immediately on mount. PodiumTVs returned early
     (empty div) while data loaded, so tv1/2/3Ref were null.
     Error: TypeError: Cannot read properties of null (reading '_gsap')
     Fix: usePodiumAnimation(ready: boolean) — effect skips if !ready.
          PodiumTVs passes ready = !!(s1 && s2 && s3). Animation fires
          once real top-3 data arrives.

  6. CSV ROW COUNT MISMATCH
     Simple line-count gave 326 rows. Correct CSV parser gave 134 rows.
     Cause: quoted fields contain embedded newlines — line-split is wrong.
     Fix: proper CSV parser that respects quoted fields.

  7. DUPLICATE EMAILS IN ROSTER CSV
     5 students submitted the Launchpad form twice.
     Fix: seed script deduplicates by email, first-occurrence wins.

  8. STUDENT COUNT MUCH LOWER THAN EXPECTED
     Plan estimated ~327 students. Actual: 129 unique students.
     Cause: the plan was based on a rough estimate, not the actual CSV.

  9. ATTENDANCE OVERWRITE BUG (rolling window)
     sync-attendance.mjs default start was (now - 14 days). The summary parser
     OVERWRITES attendance_pts with present_count * 20 each run. So after week 3,
     weekly sync would query last 14 days, get Present=2 (weeks 2-3), overwrite
     with 40 — erasing week 1's attendance entirely.
     Fix: default start changed to C7_START (2026-03-16). Edmingle summary returns
     cumulative Present count for the full queried range, so always querying from
     cohort start gives correct all-time total. Overwriting is safe.
     bulk-import.mjs also fixed: was calling sync week-by-week (each call
     overwrote with just that week's count). Now calls once: cohort start → now.

  10. DUPLICATE STUDENT RECORD (email typo)
      students table had "Rohan Kamath" twice:
        kaathrohanr@gmail.com  (typo — "kaath" instead of "kamath")
        kamathrohanr@gmail.com (correct)
      The typo entry had 60 UGC pts and 3 ugc_posts_cache rows from the UGC sync
      matching it by email. The correct entry had 40 attendance_pts.
      Fix: merged ugc_post_pts to correct record, reassigned ugc_posts_cache rows,
      deleted the typo student. Final state: kamathrohanr = 40 attendance + 60 UGC = 100 pts.
      Root cause: the Launchpad CSV had a typo email in one of two Rohan Kamath rows.
      Prevention: before seeding, validate CSV emails against known-good sources.


--------------------------------------------------------------------------------
V2 PLAN — MANUAL SCORING + ADMIN WORKFLOW
--------------------------------------------------------------------------------

  Goal: Program team can enter assignment/hackathon/capstone scores without
        touching code or SQL.

  Option A (simplest — no new code):
    Use Supabase Table Editor directly.
    Go to student_points table → find student row → edit column.
    total_points recalculates automatically.
    Suitable for small team, infrequent updates.

  Option B (recommended for scale):
    Build a minimal admin page at /admin (password-protected via middleware).

    UI: searchable student list → click student → score entry form
    Fields: assignment_pts, hackathon_pts, capstone_pts
    API: POST /api/admin/scores  { student_id, field, value }
    Auth: simple shared password in env var checked in Next.js middleware

    Files to add:
      app/admin/page.tsx
      app/api/admin/scores/route.ts
      middleware.ts  (password gate)

  Schema additions needed for Option B:
    score_audit_log (optional but recommended)
      id, student_id, field, old_value, new_value, changed_by, changed_at


--------------------------------------------------------------------------------
V2 PLAN — ATTENDANCE AUTO-SYNC VERIFICATION
--------------------------------------------------------------------------------

  RESOLVED (2026-03-17):
    - Confirmed working with real session data (Mar 13-14 sessions).
    - 270 Edmingle students parsed, 124 matched to leaderboard DB, 124 upserted.
    - 146 unmatched = Edmingle students not in leaderboard DB (different cohort
      registrations or students not in the C7 Launchpad CSV).
    - report_type=20, class_id=446933 is the correct endpoint.
    - Parser reads summary CSV (Email + Present columns).
    - attendance_pts written directly to student_points (no attendance_records).
    - Weekly GitHub Actions cron will handle ongoing automation correctly.


--------------------------------------------------------------------------------
V3 PLAN — FILTERS (ALL-TIME / WEEKLY / MONTHLY)
--------------------------------------------------------------------------------

  Goal: Users can toggle leaderboard view between time windows.

  Architecture:
    - attendance_records already has session_date — filter by date range
    - ugc_posts_cache already has posted_at — filter by date range
    - Add ?period=alltime|weekly|monthly query param to /api/leaderboard
    - API recalculates points on-the-fly for the requested window
      (do NOT store pre-computed period points — too complex to maintain)

  API change:
    GET /api/leaderboard?period=weekly
    -- calculates points only from posts/sessions in last 7 days

  Frontend change:
    - Add period toggle buttons to LeaderboardSection
    - Pass period to useLeaderboard(period) hook
    - Append ?period= to fetch URL

  Performance note:
    On-the-fly aggregation over 129 students is trivially fast.
    Only optimize if student count exceeds ~10,000.


--------------------------------------------------------------------------------
V3 PLAN — COHORT SWITCHER (C6 / C7 / future)
--------------------------------------------------------------------------------

  students table already has cohort column.
  Add ?cohort=C7 param to /api/leaderboard.
  Seed script already tags all rows with cohort='C7'.
  To add C6: run seed with a C6 CSV and cohort='C6'.
  Frontend: dropdown in navbar to switch cohort.


--------------------------------------------------------------------------------
FUTURE FEATURES (BACKLOG)
--------------------------------------------------------------------------------

  Streak display
    Show current streak badge next to student name in the table.
    Data available: Streak table in UGC tracker has currentStreak field.
    Already synced but not surfaced in UI.

  Live updates
    Currently: page fetches on load, stale after that.
    Option: add revalidate interval to useLeaderboard (e.g. every 5 min)
    Option: Supabase Realtime subscription on student_points table
    Complexity: low — single useEffect change

  Student profile page
    /student/[id] — shows full breakdown, post history, attendance record
    Data already available in ugc_posts_cache + attendance_records

  Discord bot integration
    Post weekly leaderboard update to Discord #leaderboard channel
    Trigger from sync-weekly.mjs after sync completes
    Use Discord webhook (single fetch call, no library needed)

  Edmingle webhook (if available)
    Replace weekly cron with real-time attendance sync
    Check Edmingle API docs for webhook support

  Search / filter in table
    Client-side filter by name (no API change needed)
    Add a search input above RankTable, filter STUDENTS array client-side


--------------------------------------------------------------------------------
RUNNING THE SYSTEM
--------------------------------------------------------------------------------

  One-time setup (already done):
    node scripts/seed-students.mjs

  Manual sync (run anytime):
    node scripts/sync-attendance.mjs
    node scripts/sync-ugc.mjs

  Full historical import:
    node scripts/bulk-import.mjs

  Weekly auto-sync:
    GitHub Actions → Weekly Leaderboard Sync → runs every Sunday midnight IST
    Or trigger manually from Actions tab (mode=sync or mode=bulk)

  Add manual scores (assignments/hackathons/capstone):
    Supabase → Leaderboard project → Table Editor → student_points
    Find student row → edit assignment_pts / hackathon_pts / capstone_pts
    total_points updates automatically

================================================================================
